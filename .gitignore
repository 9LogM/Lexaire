# Secrets
.env
.env.*
!.env.example

# Build artifacts
build/
build-*/
*.o
*.so
*.a
*.pyc
__pycache__/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Logs and map data (runtime)
*.log
maps/
recordings/

# Local config overrides
common/config.local.yaml

# Tests — local-only scaffolding, not tracked
python/tests/

# RS-L515-Docker is a separate git repository cloned in-place. We don't
# track it from Lexaire because (a) it has its own upstream at
# https://github.com/9LogM/RS-L515 and (b) nested-repo-as-gitlink is
# fragile without a remote. See REPORT.md "Submodule situation".
RS-L515-Docker/
