services:
  relay:
    build: .
    container_name: lexaire-relay
    network_mode: host
    restart: unless-stopped
    devices:
      - ${SERIAL_DEVICE}:${SERIAL_DEVICE}
    environment:
      - SERIAL_DEVICE=${SERIAL_DEVICE}
      - SERIAL_BAUD=${SERIAL_BAUD}
